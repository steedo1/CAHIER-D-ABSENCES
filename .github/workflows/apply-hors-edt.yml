name: Verify hors EDT runner

on:
  push:
    branches:
      - feat/class-device-hors-edt-20260915

jobs:
  verify:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: echo "runner-ok"
